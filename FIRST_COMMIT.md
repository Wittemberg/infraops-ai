# Primeiro Commit

Mensagem recomendada:

```text
docs: initialize InfraOps AI project foundation
```

Descrição sugerida:

```text
- add project vision and product positioning
- add architecture decisions and implementation roadmap
- add security and agent/action invariants
- add competitive analysis and marketing documents
- reserve monorepo structure for web, api, worker and Go agent
```

Comandos:

```bash
git init
git add .
git commit -m "docs: initialize InfraOps AI project foundation"
```

Opcional após conferir o commit:

```bash
git tag docs-v0.1.0
```
